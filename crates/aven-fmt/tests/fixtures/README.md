# Formatter Fixtures

Formatter fixtures use paired golden files.

- `valid/*.av` is the input source.
- `valid/*.fmt` is the expected formatted output.
- Each fixture must format successfully and be idempotent.
