# Temporal types: `import("std/time")` re-exports the five host-registered
# nominal types. `now`/`zone` deliberately stay bare platform globals for now —
# a host may drop the clock capability, and std must keep working without it.
{ Instant, Date, Time, DateTime, Duration }
