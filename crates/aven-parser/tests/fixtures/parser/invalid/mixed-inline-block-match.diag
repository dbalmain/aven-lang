error parse.mixed-match-arm-layout: match arms begin inline and continue on a following line
  label 32..44: this arm continues an inline arm list on a new line
  label 15..17: this `?>` starts the inline arm list
  note: write every arm inline after `?>`, separated by commas, or put every arm on its own line under `?>`
error parse.mixed-match-arm-layout: match arms begin inline and continue on a following line
  label 75..87: this arm continues an inline arm list on a new line
  label 60..62: this `?>` starts the inline arm list
  note: write every arm inline after `?>`, separated by commas, or put every arm on its own line under `?>`
