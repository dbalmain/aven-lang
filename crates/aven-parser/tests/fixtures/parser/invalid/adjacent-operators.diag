error parse.operator-fixity-undeclared: custom operator `+-` has no fixity declaration
  label 10..12: bare custom infix needs an entry fixity declaration
  note: declare `+-` in `Aven.toml`, the entry shebang, or a `--operator` argument; or rewrite this use as `left.+-(right)`
