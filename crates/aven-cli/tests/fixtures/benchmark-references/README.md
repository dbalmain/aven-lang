# Benchmark reference formatter corpus

These files mirror `aven-bench/references/*/solution.av`. The CLI formatter
integration test checks the complete corpus before formatting, after formatting,
and after a second formatting pass. Keep the snapshots in sync when the benchmark
references change.
