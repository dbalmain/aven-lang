# HTTP fetch with JSON body decoding.
# NETWORK: this example checks cleanly but is NOT run in tests.
fetchJson = (url: Text) =>
  response = Http.get(url)?^
  body = response.body.readAll()?^
  @Ok(body)
